return {
  "nvim-neo-tree/neo-tree.nvim",
  opts = {
    filesystem = {
      filtered_items = {
        visible = true, -- show hidden items
        hide_dotfiles = false,
        hide_gitignored = false,
      },
    },
  },
}
